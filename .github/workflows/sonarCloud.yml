name: Debug POM Content

on: [push, workflow_dispatch]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🔍 DIAGNÓSTICO 1 - Ver archivo real
        working-directory: ./backend
        run: |
          echo "=== CONTENIDO EXACTO DE POM.XML ==="
          cat pom.xml
          echo "==================================="

      - name: 🔍 DIAGNÓSTICO 2 - Ver qué interpreta Maven
        working-directory: ./backend
        run: |
          echo "=== DEPENDENCIAS QUE VE MAVEN ==="
          # Esto nos dirá si Maven está ignorando el bloque dependencies
          mvn help:effective-pom