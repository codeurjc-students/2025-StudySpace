## 💻 Instrucciones de Ejecución

### Prerrequisitos
* Java 17 o superior.
* Node.js (v18+) y npm.
* MySQL Server ejecutándose en el puerto 3306 (crear esquema `studyspace`).

### Ejecución del Backend
Dentro de la carpeta `backend`, ejecutar:

```bash
mvn spring-boot:run

```
### Ejecución del Frontend
Dentro de la carpeta `frontend`, ejecutar:

```bash
npm install
npm start
```